# Datos del sorteo con nombres de usuario (datos personales) — NO publicar
comentarios.csv
estado_sorteo.json
ganador.txt
mas-likes.txt

# Python
__pycache__/
*.pyc

# Datos con el TEXTO de los comentarios: no se publican
datos.json

# Token de Meta: NUNCA se commitea (dura 1-2 h, igual se borra al terminar)
.token.tmp

# Estado de las corridas: llevan ids de comentarios adentro
estado_respuestas.json
sonda_paginado.json
sorteo-artifact.html
